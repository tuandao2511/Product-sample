<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingBottom="@dimen/_8sdp"
    android:paddingRight="@dimen/_8sdp"
    android:paddingTop="@dimen/_8sdp"
    android:background="@color/color_tomoto_two">

    <androidx.appcompat.widget.SearchView
        android:id="@+id/searchProduct"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:background="@drawable/bg_search_box"
        app:queryHint="@string/message_text_search"
        app:searchIcon="@drawable/icon_search"
        app:layout_constraintWidth_percent="0.85"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <ImageButton
        android:id="@+id/buttonArrowBack"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:background="@drawable/ic_arrow_back"
        android:layout_margin="@dimen/_8sdp"
        app:layout_constraintEnd_toStartOf="@id/searchProduct"
        app:layout_constraintTop_toTopOf="@id/searchProduct"
        app:layout_constraintBottom_toBottomOf="@id/searchProduct"
        app:layout_constraintStart_toStartOf="parent"/>

</androidx.constraintlayout.widget.ConstraintLayout>
